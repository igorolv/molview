#pragma once

// Полный разбор молекулы: электронное строение каждого атома,
// трёхмерная структура, валентные углы, полярность.

#include "chem/types.h"

namespace chem {

/**
 * Порядок работы: делокализация → суммы кратностей → неподелённые пары и
 * стерические числа → направления групп → поиск циклов → построение координат
 * → разбор атомов → валентные углы → дипольный момент.
 */
Analysis analyze(Molecule mol);

}  // namespace chem
